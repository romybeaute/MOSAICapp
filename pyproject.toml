[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "MOSAICapp"
version = "0.1.0"
description = "MOSAIC: library and Streamlit app for Mapping of Subjective Accounts into Interpreted Clusters"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.9"
authors = [
    {name = "Romy Beauté", email = "r.beaut@sussex.ac.uk"},
]
keywords = [
    "topic modelling",
    "phenomenology",
    "consciousness research",
    "NLP",
    "BERTopic",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Intended Audience :: Science/Research",
]
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "scikit-learn<1.6",
    "nltk",
    "torch",
    "sentence-transformers",
    "bertopic",
    "umap-learn",
    "hdbscan",
    "huggingface_hub>=0.31.0",
]

[project.optional-dependencies]
# Streamlit interface (app.py) and its plotting stack
app = [
    "streamlit>=1.37",
    "datamapplot",
    "matplotlib",
    "gensim>=4.0.0",
    "seaborn",
    "plotly",
    "altair",
]
dev = ["pytest", "gensim>=4.0.0"]

[project.urls]
Homepage = "https://github.com/romybeaute/MOSAICapp"
Repository = "https://github.com/romybeaute/MOSAICapp"

[tool.setuptools.packages.find]
include = ["mosaic_core*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
